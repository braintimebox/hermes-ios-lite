name: HermesLite
options:
  bundleIdPrefix: thewonderofyou
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
schemes:
  HermesLite:
    build:
      targets:
        HermesLite: all
targets:
  HermesLite:
    type: application
    platform: iOS
    sources:
      - path: HermesLite
    settings:
      base:
        INFOPLIST_FILE: HermesLite/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: thewonderofyou.HermesLite
        PRODUCT_NAME: HermesLite
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
